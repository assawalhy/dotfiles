# TODO

- [x] Write setup/steps/65-beyond-compare.sh (per-platform branches + repo guards)
- [x] bash -n + shellcheck clean
- [x] setup-os --list and --dry-run show the step entry
- [ ] Live-run on this Ubuntu box (skipped on request -- run the step manually)
- [ ] Commit `setup: add Beyond Compare install step`
