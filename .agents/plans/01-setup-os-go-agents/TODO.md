# TODO

- [x] Make go step p2 instead of p1 in 05-golang.sh
- [x] Verify glow stays p2 in packages.list
- [x] Add npm section to setup-os for npm-based agents
- [x] Add coding agents to packages.list (pi in [go]; codex, kilo in [npm]; opencode/claude via existing steps)
- [x] Verify setup-os --list shows all new entries correctly
