## 2026-08-14T20:59Z Work: link-refresh-audit started (orchestrator: Atlas, session opencode:ses_00ddf4af8ffeZnpdT6J6zlib6H)
- HEAD = 92f956c. Plan commits land on top of it, one per todo, repo style "Area: description".
- Working tree MUST stay `MM .gitignore` + `?? .omo/` at all times. NEVER `git add .gitignore`, `.codegraph/`, `.claude/`, `.omo/notepads`.
- Evidence: `git add -f .omo/evidence/task-<N>-link-refresh-audit.txt` (force-add; staged ignore covers .omo/). Append via cat >>.
- Fixture recipe: fx-repo = cp -r of `link-files.bash link-ignore.txt .gitignore common linux macos` (NO .git; link-context.txt absent until T2); run fixture script with HOME=/tmp/opencode/fx-home so REPO resolves to fixture (script derives REPO from own location, link-files.bash:29-35).
- bash 3.2: no readarray/mapfile, no declare -A, no ${v,,}, no globstar, no sort -V, no find -mindepth. set -e-safe guards at :157, :367-368.
- Live junk to exclude via gitignore: ~/.config/ranger/plugins/__pycache__/, ~/bin/.vscode/launch.json. Real refresh candidate: ~/.config/nvim/lua/plugins/kotlin.lua.

## 2026-08-15 T1 --refresh findings (executor: Sisyphus-Junior)
- WORKED: deriving linked dirs from the overlay-merged rel set (dirnames of rels containing '/') + collapsing nested dirs to their shallowest ancestor (single awk after sort -u: 'index($0, prev "/") != 1') is exactly what prevents double-find/double-mv. $desired (built in collect) doubles as the in-repo rel set (grep -Fxq, same idiom as find_stale:368) -- no new temp files needed, all trap-managed by collect.
- WORKED: mirror-root in refresh_scan is just 'OSDIR dir exists ? OSDIR : COMMON' (the elif-else both go to common). newfile.lua/extra.lua -> common/.config/nvim; new.sh -> linux/.config/shell (overlay) proven in QA.
- WORKED: fail-closed gitignore guard 'if git check-ignore ...; then continue; elif [ $? -ne 1 ]; then continue; fi' -- exit 0 ignored/skip, exit 1 keep, any git error skip (never capture unprovable files).
- GOTCHA (fixture): plan said fixture repo has NO .git and that 'git check-ignore --no-index' works without one -- FALSE on git 2.43.0 ('fatal: not a git repository', exit 128). Fix: git init the fixture (no commits needed but I committed the initial state so 'git status --short of fx-repo' is clean evidence). The plan's evidence requirement itself implies a repo.
- GOTCHA (commit hygiene): the pinned 'MM .gitignore' means .gitignore ALREADY has staged hunks in the index; a bare 'git commit' sweeps them in. First commit did; fixed with 'git reset --soft HEAD~1' (index untouched -> MM restored) + pathspec commit '-- link-files.bash .omo/evidence/task-1-link-refresh-audit.txt'. ALWAYS commit by pathspec on this repo.
- GOTCHA (preview format): plan's implementation bullet says '[$refresh -> common|linux|macos]' but the acceptance criterion greps '+  <rel> [refresh]'. Resolved with '+  <rel> [refresh] -> <root>' -- the literal acceptance string is a prefix of the line AND the destination is shown.
- GOTCHA: gawk warns 'escape sequence \. treated as plain .' on any '\.lua$' pattern -- PRE-EXISTING (normal flow emits it too); harmless stderr noise, exit 0.
- GOTCHA (pattern vs dir set): linked dirs MUST come from the FULL overlay rel set, not the pattern-filtered merged. If collect applied the pattern first, '--refresh \.lua$' would scan only dirs holding .lua repo files (e.g. .config/nvim/lua/plugins) and miss newfile.lua in .config/nvim. Fixed in collect: '!seen[$2]++ && (all == 1 || $2 ~ pat)' with all=$is_refresh.
- BASH 3.2: no new constructs; bad-regex '[[ $rel =~ [ ]] || continue' under set -e is safe (compound returns 0, candidate skipped) -- verified under real bash.
- NEW CODE (committed file): refresh section header :387, rfr arrays :393, refresh_scan :395-445, refresh_confirm :447-471, refresh_apply :473-488 (rollback on ln failure :480-486), collect pattern gate ~:159-165, parse_args --refresh :92, print_help :50-52/:63-65/:69, main picker bypass + refresh branch :614-625.
- COMMIT: 6b4c3ae 'link-files: add --refresh to capture new files in linked dirs' (link-files.bash + evidence only; .gitignore stays MM).

## 2026-08-15 T2 --audit findings (executor: Sisyphus-Junior)
- WORKED: neglect predicate split via ${line%%: *} / ${line#*: } (first ': ' only) is literal-safe -- no glob/regex pitfalls, `=` compares exact relpaths. M7 pin verified: dual-context rel reported in both listed sessions, neglected in headless; .Xmodmap inversion (x11 reports, wayland suppresses) proved not double-negated.
- WORKED: session_context env order (WAYLAND_DISPLAY first) matches live machine (wayland-0). x11 fixture run proves Xwayland-both-set case.
- WORKED: read_contexts writes parsed lines to $CTX_TMP with sed -e '/^[[:space:]]*#/d' -e '/^[[:space:]]*$/d' -e '/: /!d' -- the third expr drops malformed lines (no ': '). Missing link-context.txt = empty neglect (read_contexts returns before sed), no error.
- WORKED: audit reuses classify/find_stale arrays + refresh_scan candidates read-only; collect gate extended to all_flag=$is_refresh$is_audit (0/1, NOT string concat -- awk 'all == 1' coerces "11" to 11 and breaks). T1 --refresh pattern regression re-run: still lists only .lua candidates.
- GOTCHA (fixture, IMPORTANT): do NOT put the foreign-symlink fixture element at $HOME/.gitconfig (or any path git reads as config). git check-ignore reads $HOME/.gitconfig; a symlink there -> /etc/passwd makes git exit 128 'fatal: bad config line 1' and the fail-closed guard silently skips every refresh candidate (no [unlinked] lines). Move foreign symlink to e.g. .config/mpv/mpv.conf. The script behavior was CORRECT (fail-closed); the fixture was wrong.
- GOTCHA: zsh shell for bash tool -- PIPESTATUS is $pipestatus (lowercase, 1-based); `${PIPESTATUS[0]}` reads grep's exit, not the script's. Capture audit exit codes with a bare `echo $?` after the command, not through a pipe.
- GOTCHA: my wrapper used `set -e` and the audit's exit 1 (findings) aborted the wrapper before the next echo. Audit exits 1 BY CONTRACT on findings -- never wrap fixture runs in set -e.
- GOTCHA (bad pattern '[invalid'): silently filters everything -> 'Audit clean (N links correct, M neglected)' exit 0. Consistent with T1 --refresh bad-regex behavior (no candidates). Recorded as adversarial probe, not a bug.
- NEW CODE (committed 379ee4c): CTX_FILE :40, is_audit decl :46, parse_args --audit :101, collect all_flag+trap :160-178, audit section header :502, session_context :508-515, read_contexts :519-526, neglected :532-542, audit :544-612, main audit branch :752-757.
- COMMIT: 379ee4c 'link-files: add --audit link-drift report with context neglect' (link-files.bash + link-context.txt + evidence, pathspec; .gitignore still MM).

## 2026-08-15 T3 --no-backup findings (executor: Sisyphus-Junior)
- WORKED: dir-refuse guard is gated on is_no_backup (NOT unconditional in the backup branch): plain `--force` on a directory conflict keeps the old `mv` backup behavior (mv CAN move a dir; that is pre-existing, MUST-NOT-change behavior). The refuse fires only for `[ -n "$is_no_backup" ] && [ -d "$2" ]`, ordered BEFORE the is_no_backup rm path, so a dir under --no-backup errors rather than being rm'd (Metis M4: plain `rm -f` on a dir fails -> set -e abort mid-apply).
- KEY INSIGHT (acceptance 4 semantics): the dir-refuse `[ -d "$2" ]` can ONLY fire when a real DIRECTORY sits at a repo FILE relpath (e.g. `$HOME/.config/mpv/mpv.conf` as a dir while `common/.config/mpv/mpv.conf` is a repo file). classify emits FILE rels only (list_root is `find -type f`), so for a real dir at a repo DIR relpath (`.config/nvim`), $2 is always a FILE inside it and the guard never fires. PROBED: real `fx3-home/.config/nvim` with `init.lua` under `--force --no-backup --yes '\.config/nvim'` -> exit 0, directory SURVIVES (still a real dir), each repo file symlinked inside it, the conflicting real init.lua replaced (rm'd + re-linked). That is the flag's per-file contract and honors 'never remove a directory'; it is NOT the dir-refuse case. For a genuine guard exercise, acceptance 4 uses a real dir at a repo FILE relpath (`$HOME/.config/mpv/mpv.conf` as a dir): classify -> hard_rel -> backup branch -> `[ -d ]` refuse -> dir error, exit 1, dir + contents intact, no .bak, no symlink (no partial apply). If a later verifier uses the plan's literal `.config/nvim` example they will NOT see the dir error -- point them at this note + the evidence.
- WORKED: parse_args error check sits AFTER the `is_help` branch (so `--help` still wins) and before `filter` defaulting: `[ -n "$is_no_backup" ] && [ -z "$is_force" ]` -> printf 'error: --no-backup requires --force\n' >&2; exit 1. Fires before collect/classify/apply -> nothing is ever written (verified: file unchanged, no .bak, no symlinks).
- GOTCHA (my own QA script): probing with `... | head -20` SIGPIPE-killed the script mid-apply under set -e (init.lua stayed a real file) AND `$?` after the pipe was head's exit 0 -- the T2 pipe-exit gotcha again, this time self-inflicted. Re-ran the probe unpiped: exit captured as 0 (script's own), apply completed. NEVER truncate a fixture run through head; write to a file and head the file.
- VERIFIED regressions: `--refresh --dry-run 'extra\.conf$'` on a linked-dir fixture still lists `+  .config/mpv/extra.conf [refresh] -> common`, exit 0, nothing moved. `--audit 'mpv\.conf$'` clean -> 'Audit clean (1 links correct, 2 neglected)' exit 0; full `--audit` -> findings, exit 1, read-only (symlink intact after). `--help` lists --no-backup (USAGE line + OPTIONS block).
- CONSTRUCTS: no new bash 3.2 violations (scan of added lines: no <<<, mapfile, readarray, declare -A, ${v,,}, globstar, sort -V, find -mindepth). `bash -n` clean.
- NEW CODE (uncommitted): is_no_backup decl :46, help USAGE :51 + OPTIONS :63-65, parse_args case :105 + requires-force check :122-125, link_one backup branch :705-719.
- COMMIT (this todo): `link-files: add --no-backup to skip .bak under --force` (pathspec: link-files.bash + force-add evidence only; .gitignore stays MM).

## 2026-08-15 T4 README findings (executor: Sisyphus-Junior)
- VERIFIED: `./link-files.bash --help` USAGE block + OPTIONS block were mirrored verbatim into the README "Linking" usage block. diff of the 4 USAGE lines and the option lines against help output: identical (README's only structural diffs are the "OPTIONS:" header position and the intentionally omitted INTERACTIVE SELECTION / MARKERS / runtime "Symlinks...Detected OS" lines -- the plan scopes the mirror to "USAGE line + OPTIONS block").
- WORKED: placing the two new subsections ("Capturing new files (--refresh)" then "Checking the link state (--audit)") between "What gets linked" and "Conflicts" keeps the plan's listed order and reads naturally (link -> capture -> audit -> conflicts). Fence balance preserved: 7 code blocks, 14 fence lines, even.
- ACCURACY PIN (from T2 evidence + F3 spec): `.Xmodmap` is NEGLECTED on wayland (not reported) and REPORTED on x11. The audit subsection says "skipped on a Wayland session and reported on an X11 one" -- do not invert this in later edits. The real machine (wayland-0) audit must NOT list .Xmodmap/.xinitrc (F3 asserts this).
- Confirm prompt is `🔥 Are you sure? [y/N]` (link-files.bash:486 and :680) -- the refresh subsection mentions "asks for the 🔥 confirmation" matching it, with the emoji used ONLY there per plan.
- GOTCHA (shell quoting, not a bug): `grep -c '^```' README.md` written inside a `$(...)` in a zsh command string collapsed the pattern (returned 250, the whole file). Real count via awk: 14, even. Any later verifier must count fences with awk or a heredoc'd script, not inline `$(grep -c '^\`\`\`' ...)`.
- CONFIRMED: the plan's conflict-amended sentence reads "...unless `--no-backup` is given, which deletes the real file instead (and refuses directory conflicts)." -- matches T3's semantics (dir-refuse only under --no-backup, requires --force).
- COMMIT (this todo): `README: document --refresh, --audit and --no-backup` (pathspec: README.md + force-add task-4 evidence only; .gitignore stays MM; pre-existing `M .omo/evidence/task-1-link-refresh-audit.txt` untouched).

## 2026-08-15 F4 findings (reviewer: F4 scope-fidelity)
- VERDICT: APPROVE. `git diff 92f956c..HEAD --name-only` = exactly 7 files: link-files.bash, link-context.txt (new, 6 + / 0 -), README.md, .omo/evidence/task-{1,2,3,4}-link-refresh-audit.txt. Nothing else.
- Guardrail dirs clean: `git diff 92f956c..HEAD -- common/ linux/ macos/ setup/ setup-os` = empty output. No .gitignore in the committed diff (empty output for that pathspec; working tree still `MM .gitignore`). No `.ignore`/`.bak`/tmp files anywhere in diff or status.
- Commit chain (exactly 4, messages match plan 200-204 verbatim): 6b4c3ae (T1 refresh), 379ee4c (T2 audit + link-context.txt), 27def25 (T3 no-backup), fd7a8c5 (T4 README, rewritten from ced8606 as expected). Per-commit file lists verified with `git show --name-only`: each commit contains only its own script/data/README + its own evidence file. No cross-contamination (T1 commit-sweep gotcha NOT repeated).
- `.codegraph/`/`.claude/` not tracked (`git ls-files | grep -E '^\.codegraph/|^\.claude/'` = no matches).
- Working tree outside `.omo/`: only `MM .gitignore` + ` M .omo/evidence/task-1-link-refresh-audit.txt` (pre-existing expected). All `??` entries are under `.omo/` (notepads/drafts/plans/boulder/run-continuation/start-work + os-setup-customization evidence) = expected untracked state.
- Guardrails: no `--reverse` restoration (diff only shows the pre-existing removed-flag error stub + comment text, unchanged lines); link-context.txt is a flat 6-line `context: relpath` list (no globs/dir semantics); README diff = 4 hunks, all in Linking/usage/Conflicts areas (USAGE/OPTIONS block, "What gets linked" neglect sentence, the two new subsections between "What gets linked" and "Conflicts", and the --no-backup amendment in Conflicts). No unrelated-section restructuring. Neglect wording correct: .Xmodmap skipped on wayland, reported on x11 (no T4 inversion).

## 2026-08-15 F2 findings (reviewer: F2, code quality gate; base 92f956c, HEAD fd7a8c5)
- VERDICT: APPROVE. Full diff scanned line-by-line; no bash 3.2 violations, no dead code, bash -n clean, set -e guards complete, no fall-through.
- FORBIDDEN-CONSTRUCT SCAN (exact command from F2 spec, added lines of full diff): the ONLY matches were 2 documentation lines in .omo/evidence/task-1-link-refresh-audit.txt (the T1 scan-result log quoting the construct names) -- NOT code. Re-ran on added lines of link-files.bash + README.md + link-context.txt only: zero matches. Whole-file grep of link-files.bash matches only the pre-existing header comment line 11 (construct ban note, present in base) -- not introduced by this plan.
- REACHABILITY: refresh_scan (main :765 refresh branch + :774 audit branch), refresh_confirm (:766), refresh_apply (:767), session_context (:546 in neglected, :559 in audit), read_contexts (:773), neglected (5 call sites :579/:586/:593/:600/:609), audit (:775). All 7 new functions reachable from main; no unreferenced vars (all_flag, is_refresh/is_audit/is_no_backup, rfr_* arrays, CTX_TMP all consumed; CTX_TMP also in collect trap :171).
- --REVERSE: only the documented error path in parse_args (:106-110: two printf lines + exit 1, nothing else). Header comment :8 "see --reverse below" refers accurately to that case. fzf --reverse at :315 is fzf's layout flag (pre-existing, unrelated). README:111 documents the removal. No restored --reverse behavior.
- SYNTAX: `bash -n link-files.bash` PASS at HEAD. README fences 14, EVEN (base 12 + the 2 new blocks) -- counted with awk per T4's zsh gotcha. link-context.txt: 3 whole-line comments + `x11: .Xmodmap` / `x11: .xinitrc`, format `<context>: <relpath>`, no globs -- matches spec exactly.
- SET -E SAFETY (every new pipeline): refresh_scan grep/awk/git-check-ignore all in `if ...; then continue; fi` or the fail-closed `elif [ $? -ne 1 ]` guard (:440-444); `[[ $rel =~ $filter ]] || continue` (:450); find in `< <(...)` with the `|| [ -n "$f" ]` read guard (:429, :459); refresh_apply rollback `if ! ln -s` + mv-back + exit 1 (:501-506); read_contexts `sed ... || :` (:537); neglected/audit guarded reads + `if neglected` + `|| continue`; parse_args error paths (:110/:112/:115/:124) exit 1 before any file I/O; link_one dir-refuse (:711-714) fires before the is_no_backup rm, honoring T3's M4. The bare `mkdir -p` in refresh_apply aborts pre-mv on failure (user file untouched) -- acceptable.
- MUTUAL EXCLUSIVITY: refresh branch (:764-769) runs scan/confirm/apply then exit 0 BEFORE classify; audit branch (:772-777) runs classify/find_stale + read_contexts/refresh_scan/audit then exits (0/1 inside audit :616/:619); picker gate excludes both (:760-761); neither reaches confirm/apply. --refresh+--audit together: refresh wins, still exits cleanly, no fall-through. --no-backup alone (no --force) exits 1 in parse_args.
- BASh 3.2 MENTAL MODEL: only process substitution `< <(...)` used (fine in 3.2); `local neglects=()` / global `rfr_*=()` array init, `+=` append, `${#arr[@]}`, C-style for, `=~` with unquoted RHS -- all 3.2-safe. No mapfile/readarray/declare -A/${v,,}/globstar/sort -V/find -mindepth anywhere in the added code.
- No committed-file modifications made; nothing staged/committed (read-only review). Working tree remains pinned MM .gitignore + ?? .omo/ as expected.

## 2026-08-15 F3 findings (reviewer F3, Sisyphus-Junior)
- VERDICT: APPROVE. All fixture recipes + the read-only real-home smoke pass on committed state HEAD fd7a8c5. Full commands/outputs in .omo/evidence/f3-link-refresh-audit.txt.
- --refresh: dry-run lists newfile.lua + lua/extra.lua -> common and new.sh -> linux overlay; --yes moves + symlinks back; junk.pyc/skip.me skipped; read-only dry-run (sentinel find -newer empty); pattern filter; confirm-n exit 1 no moves. All exit 0. Overlay mirror rule CONFIRMED (linux/.config/shell wins).
- --audit env matrix: wayland (both WAYLAND_DISPLAY+DISPLAY set) suppresses .Xmodmap/.xinitrc; x11 reports them (header becomes 'audit context: x11', .xinitrc [missing] + .Xmodmap [conflict]); headless suppresses. Exit 1 findings / exit 0 clean (clean fixture: 'Audit clean (69 links correct, 0 neglected)'). Read-only proven (sentinel empty, symlinks unchanged). Missing link-context.txt -> no error, reports x11 files. Malformed line ignored; 'wayland: X' line parsed (reported in wayland, suppressed in headless -- M7 semantics confirmed).
- --no-backup: --force --no-backup --yes replaces real file, no .bak.*; plain --force --yes keeps .bak.<STAMP>; bare --no-backup -> 'error: --no-backup requires --force' exit 1 nothing changed; real DIR at repo FILE relpath (mpv.conf as dir) -> dir error exit 1 dir intact. Repo-DIR-relpath case (.config/nvim real dir) = per-file replacement, dir survives (matches T3 note -- do NOT use the plan's literal .config/nvim example to test the dir-refuse).
- REAL-HOME SMOKE: git status --short 25 lines IDENTICAL before/after; ./link-files.bash --audit exit 1; header 'wayland (neglecting: x11: .Xmodmap, x11: .xinitrc)'; 61 '!' conflicts + 15 '+' unlinked = 76 findings. .Xmodmap/.xinitrc ABSENT from findings. Conflicts are genuine (verified on disk: .gitconfig/.zshrc/.tmux.conf/.config/nvim/init.lua are real files). kotlin.lua correctly [unlinked] (known live candidate). No .bak.* created.
- DELTA vs plan: conflict count 61 vs expected ~45. Acceptable (plan: range ok, key = findings exist + real conflicts listed). Live machine drift > plan's estimate; not a defect.
- OBS (not a defect): a concurrent final-wave agent staged .omo/ files (git status showed 'A' entries mid-smoke) -- the .omo/ staged-ignore interplay means '?? .omo/' vs 'A .omo/*' depends on other agents' git add timing; F3's gate is strictly before/after identity of MY audit run, which held.
- GOTCHA (mine): rebuilding a fixture home by writing THROUGH existing symlinks re-created repo files instead of home real files ('Nothing to refresh.' was correct skip of already-in-repo). Rebuild BOTH home and repo for a clean pattern/confirm test.

## 2026-08-15 F1 findings (final-wave plan compliance audit)
VERDICT: APPROVE
- Commit chain verified: `git log --oneline 92f956c..HEAD` = exactly 6b4c3ae (T1 --refresh), 379ee4c (T2 --audit + link-context.txt), 27def25 (T3 --no-backup), fd7a8c5 (T4 README); HEAD = fd7a8c5. Each commit contains ONLY its script/data/README + the matching evidence file (verified via `git show --stat`); .gitignore never committed; link-context.txt content matches the plan verbatim (x11: .Xmodmap / x11: .xinitrc).
- Plan file: all 4 todo checkboxes `- [x]` present (lines 74, 107, 150, 171). Evidence files all present on HEAD: .omo/evidence/task-{1,2,3,4}-link-refresh-audit.txt.
- Working tree files (link-files.bash, README.md, link-context.txt, link-ignore.txt) are byte-identical to HEAD (`git diff HEAD --stat` empty). Pinned MM .gitignore + ?? .omo/ state respected; a concurrent F3 run staged .omo/ files during my audit (expected parallel activity, did not touch committed content).
- T1 re-run (fresh f1t1 fixture, git-init'd repo, `.config/nvim/skip.me` in link-ignore, __pycache__/junk.pyc gitignored):
  - `--refresh --dry-run`: prints `+  .config/nvim/newfile.lua [refresh] -> common`, `+  .config/nvim/lua/extra.lua [refresh] -> common`, `+  .config/shell/new.sh [refresh] -> linux`; NOTHING for junk.pyc/skip.me; exit 0; no files created (sentinel find clean).
  - `--refresh --yes`: repo holds the 3 files (new.sh under linux/, not common — overlay mirror), fx-home readlinks == fx-repo paths, originals gone, init.lua symlink untouched, junk.pyc/skip.me still real; exit 0; fx-repo git status shows exactly the 3 untracked files.
  - `--refresh '\.lua$' --dry-run`: only the 2 .lua candidates, exit 0. Confirm prompt `printf 'n\n'` → exit 1, nothing moved.
  - Rollback QA: stubbed ln (exit 1) → `error: ln failed ...; file moved back`, file back in home, absent from repo, exit 1.
  - FIXTURE GOTCHA (mine): appending bare `skip.me` to link-ignore does NOT ignore `.config/nvim/skip.me` — the predicate is `rel == $0 || index(rel, $0 "/") == 1`; the entry must be the full relpath `.config/nvim/skip.me`. With the correct entry it is excluded. Not a script bug.
- T2 re-run (fresh f1t2 fixture: correct symlinks, missing, stale (committed then deleted from fx-repo), foreign symlink at .config/mpv/mpv.conf (NOT .gitconfig — documented gotcha), real-file conflicts (.zshrc, .Xmodmap), unlinked candidate, x11: .Xmodmap/.xinitrc in link-context):
  - wayland (WAYLAND_DISPLAY=wayland-0 DISPLAY=): header `audit context: wayland (neglecting: x11: .Xmodmap, x11: .xinitrc)`; .Xmodmap/.xinitrc NOT reported; stale/missing/conflict/unlinked all reported; exit 1.
  - x11 (WAYLAND_DISPLAY= DISPLAY=:0): reports `+  .xinitrc [missing]` and `!  .Xmodmap [conflict]`; no neglect header; exit 1.
  - headless: both neglected, other findings reported, exit 1.
  - Read-only sentinel: `find fx-repo fx-home -newer <sentinel>` EMPTY after an audit run; fx-repo git status clean; exit codes captured via bare `echo $?` (not pipes).
  - Clean audit (fx2, all linked): `Audit clean (69 links correct, 0 neglected).`, exit 0.
  - Missing link-context.txt: removed from fx-repo → no error, `audit context: x11`, .Xmodmap reported as conflict, exit 1.
- T3 re-run (fresh f1t3 fixture, real file at .config/mpv/mpv.conf):
  - `--force --no-backup --yes 'mpv\.conf$'` → symlink replaces real file, `find -name '*.bak.*'` count 0, exit 0.
  - `--force --yes 'mpv\.conf$'` → `.bak.20260815004621` present, exit 0.
  - bare `--no-backup` → `error: --no-backup requires --force`, exit 1, file unchanged, no .bak, no symlinks.
  - dir conflict (real DIR at .config/mpv/mpv.conf while common/.config/mpv/mpv.conf is a repo file) → `error: --no-backup cannot replace a directory (...); run --force without --no-backup or remove it manually`, exit 1, dir + contents intact, no .bak, no partial apply.
  - Notepad's dir-relpath nuance confirmed: the guard only fires for a real dir at a repo FILE relpath.
- T4 re-run (committed README + --help):
  - `grep -c -- '--refresh' README.md` = 9 (>=3); `--audit` = 7 (>=3); `--no-backup` = 3 (>=2). (rtk grep wrapper mangles `--`-prefixed patterns — must use the raw /usr/bin/grep binary.)
  - `grep -q 'link-context.txt'` PASS; `grep -q 'no-backup'` PASS; `--reverse` removal note + "no longer needs node" claim intact.
  - Fence count via awk = 14, EVEN.
  - `./link-files.bash --help | grep -q -- '--refresh'|'--audit'|'--no-backup'` all PASS (USAGE + OPTIONS blocks).
  - `bash -n link-files.bash` on committed state: PASS.
- Every acceptance criterion in the plan (todos 1-4, final-wave F1 list) re-verified from the committed state, independent of the orchestrator's verdict. Evidence files are consistent with observed behavior (re-ran dry-run/apply/pattern/confirm/rollback for T1, the full env matrix + sentinel + clean + missing-ctx for T2, all four no-backup acceptances for T3, and the T4 greps/fence/help).
