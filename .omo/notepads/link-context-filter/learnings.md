# learnings
- task-1 (2026-08-15): plan line refs for collect()/trap/read_contexts/main all matched the CURRENT file exactly (no drift) — trap at 174, merged 190-192, desired 194, mdirs 200-203, read_contexts 550-555, session_context 537-545, audit-path call 828, main 810-834.
- The working tree had pre-existing --diff/--no-backup hunks in link-files.bash (is_diff var/USAGE/OPTIONS/parse_args, picker rewrite, classify no-backup, show_diff/show_diffs/preview). 12 total hunks; git add -p with answers n n n y y y n n n n y y staged only the 5 context-filter hunks. Commit e61e3bb.
- Fixture quirk: link-context.txt MUST be committed in the fixture repo (R7 deletes it; rebuilding restores it).
- On wayland, .multi (listed wayland+x11) is never neglected — listed in dry-run and audit. The audit header still shows "neglecting: x11: .multi" — accepted pre-existing multi-context header quirk (todo 3 documents it).
- R6 with pattern '.*Xmodmap' on wayland prints "Nothing to do (0 links already correct)." (not literally "Nothing to link") — script's own empty message, exit 0, nothing created.
- bash -n must be re-run against the fixture copy AFTER copying the edited script in (fixture copies at build time).
- task-2 (2026-08-15): refresh_scan() drift vs plan — plan said rel at :447 / desired check :449 / dir list :477-481; CURRENT file had rel at 465, desired at 467, dir list 495-499 (10 lines drift from the 20-line NEG_RELS block added by todo 1). The insert point (after rel=, before desired check) was re-anchored by grep and matched the plan's relative ordering exactly.
- The plan's ".newnormal" normal-capture probe is INVALID: top-level rels are never scanned by refresh_scan (parent = $HOME is skipped, "Top-level rels ... $HOME is never scanned"). A top-level file is not refresh-capturable in the base design. Proved normal capture instead with a new file inside a wayland-scanned dir (.config/misc/newfile after adding a neutral repo dir). Not a regression — documented in evidence task-2.
- On this fixture, wayland has NO scanned dirs by default (all recipe dirs are x11-neglected); the neutral-dir addition is required for the normal-capture probe.
- 8 hunks in link-files.bash at todo 2 commit time; only hunk 6 (refresh_scan) staged. Commit af7670f (2 insertions).
- task-3 (2026-08-15): audit()/neglected() drifted ~24 lines from plan refs (plan said audit :574-637, neglected :557-572; CURRENT was audit 594-657, neglected 580-592 after todos 1-2). Re-anchored with grep -- the relative structure matched exactly.
- grep acceptance nuance: `grep -n 'neglected'` -> no matches cannot be literal while the todo-2 comment "# neglected for this session" (plan-mandated verbatim) exists. Resolved: reworded MY OWN comments (todo-1 collect comment "neglected-but-linked" -> "filtered-out-but-linked"; todo-3 doc comment "neglected rels/candidates" -> "neglect-listed rels/candidates"); kept the todo-2 comment verbatim. Final: 1 comment occurrence (todo-2), 0 code refs, 0 added lines contain 'neglected'. Documented in evidence.
- R8 verified the accepted context-independence: stale link (deleted repo target) reported on BOTH wayland and x11 even though .Xmodmap is x11-tagged -- find_stale detects the dangling repo-pointing symlink via $desired/$mdirs regardless of the neglect filter. exit 1 both.
- R13 setup: link wayland-visible rels via `link-files.bash --yes` under wayland env (x11 rels never linked because collect filters them). Audit clean (2 links correct). N=2 = .multi + .zshrc.
- 12 hunks at todo-3 commit time; staged 5 (collect comment reword, doc comment, neglected() deletion, 2 audit-body hunks). Commit 5572084 (6+/27-).
- task-4 (2026-08-15): docs only — link-context.txt header, README (OPTIONS + What gets linked + --refresh + --audit sections), print_help() OPTIONS lines. Committed eadf053 (34+/19-). Acceptance: no 'for --audit' in link-context.txt; 4 'neglect' matches in README all new wording; --help/README OPTIONS blocks byte-identical (diff of extracted ranges).
- git add -p with a piped answer stream DRIFTED TWICE on the big OPTIONS hunk: `s` split it into THREE hunks (--diff block / --refresh / --audit), not two — the third hunk consumed the next answer meant for a later hunk. Fixed by `git reset <file>` + re-running with the corrected stream. Lesson: after any `s`, verify `git diff --cached` before committing; when in doubt reset and redo.
- pre-existing uncommitted --diff work is extensive in BOTH link-files.bash (is_diff var/USAGE/OPTIONS/parse_args/picker rewrite/classify/show_diff/preview) and README.md (USAGE [--diff], --diff --dry-run line, --diff OPTIONS block, Interactive selection rewrite, --diff Conflicts paragraph). Left unstaged at todo-4 commit; still in the working tree for the future --diff commit.
- task-5 (2026-08-15): full env-matrix QA sweep R1-R13, evidence .omo/evidence/task-5-link-context-filter.txt (1404 lines, 13 PASS rows, 0 FAIL). Sweep harness /tmp/opencode/task5-sweep.sh: rebuilds the fixture FRESH per state-changing row (recipe lines 74-103, git init), runs each invocation with HOME=/tmp/opencode/fx-home + env row, captures output AND echo $? into the transcript, asserts presence/absence + rc live, emits PASS R<n>. Summary: "Rows passed: 13  Rows failed: 0".
- All 13 rows verified live: R1 wayland dry-run/audit (x11 rels absent, .zshrc+.multi present); R2 correct-neglected symlink not stale-removed; R3 real-file conflict invisible under --force --no-backup; R4 x11 full list incl .Xmodmap; R5 headless = wayland for x11 rels (audit findings 1); R6 '.*Xmodmap' -> "Nothing to do (0 links already correct)." exit 0 nothing created; R7 deleted link-context.txt -> all 6 rels listed (guard holds); R8 stale-deleted reported on BOTH wayland (3 findings) and x11 (6 findings); R9 neutral-dir probe captured on wayland + newfile only on x11; R10 "Nothing to refresh." on wayland (zero scan dirs) + new2 only on x11; R11 .multi [missing] both sessions; R12 --diff --dry-run shows .zshrc diff, no .Xmodmap; R13 Audit clean (2 links correct). no neglected suffix, exit 0.
- Harness bug caught in review: my no()/bad() helpers printed FAIL but did NOT increment the row-fail counter, so every row would have passed regardless — fixed bad() to bump ROW_FAIL. Second bug: audit "does not report X" assertions pattern-matched the HEADER line ("audit context: wayland (neglecting: x11: .Xmodmap, ...)") which legitimately names neglected rels -> false FAILs. Fixed by anchoring finding-line patterns as ^[+*!~-][[:space:]]+.*\\.Xmodmap. Lesson: audit absence assertions must match finding lines only, never the header.
- The interrupted first attempt left only raw script output in /tmp/opencode/task5/*.out (no exit codes, no env/invocation record); the evidence file was never written and the fixture sat in the R13 end-state. Salvage value: the 26 .out transcripts were verified line-by-line against the row specs and the fresh sweep re-confirmed them exactly (only diff timestamps differ). The task instruction "salvage rather than re-run" maps to: verify the .out files are CORRECT, then the fresh sweep re-captures them WITH exit codes + env + invocation for the evidence.
- R9/R10 confirmed the fixture rebuilds differ: R9 uses the neutral-dir variant (common/.config/neutral/probe committed; home probe .config/neutral/newprobe captured -> wayland scan-dir exists), R10 uses the PLAIN recipe (wayland refresh = "Nothing to refresh." because zero scan dirs survive the filter). The neutral probe must NOT be present in the R10 build or the wayland refresh/audit would list probe/unlinked noise.
- Script SHA-256 recorded in evidence: 1fa78165957fcb91a7a3e9bd3f309dbbfe7050e162b4003e2e248ebc2e4102c9 (working-tree copy incl. uncommitted --diff hunks; R12 depends on --diff).
- task-6 (2026-08-15) F4 scope fidelity: VERDICT APPROVE. Base confirmed e61e3bb^ = 01c9304. `git diff --name-status 01c9304..HEAD` = exactly M README.md, M link-context.txt, M link-files.bash. Per-commit: e61e3bb 20+/2- bash only, af7670f 2+ bash only, 5572084 6+/27- bash only, eadf053 README+link-context.txt+bash (bash hunk = print_help OPTIONS only). Zero .omo/ paths in all 4 commits. Committed link-files.bash diff has exactly 2 'diff' matches (the diff --git header + the "every mode (link/list/diff/refresh/audit)" comment) — no --diff feature hunks leaked. Unstaged residue intact: 15 --diff/no-backup markers in link-files.bash, 11 in README.md; git diff --cached empty (no half-staged leftover). boulder.json last touched by 7f0046c (pre-base); ctx2.txt never tracked — both observations, not findings.
- task-7 (2026-08-15) F3 manual QA: VERDICT APPROVE. All 6 rows PASS live on tmux: R1 wayland dry-run lists only .multi+.zshrc (rc 0) and audit reports only those [missing] (rc 1, no x11 finding lines); R6 '.*Xmodmap' -> "Nothing to do (0 links already correct)." rc 0 nothing created; R7 deleted link-context.txt -> all 6 rels listed rc 0 (guard holds); R8 stale symlink reported on BOTH wayland (3 findings) and x11 (6 findings) + dry-run shows '-' (context-independent); picker on wayland via real tmux session showed .multi/.zshrc 2/2 with NO x11 rels, aborted cleanly ("Nothing selected.") with fx-home untouched; real-repo --audit on wayland env printed "audit context: wayland (neglecting: x11: .Xmodmap, x11: .xinitrc)" rc 0, git status byte-identical before/after, real $HOME snapshot diff = only this session's own opencode/claude transcript/log/db artifacts (0 dotfile writes). Picker quirk observed: fzf has NO default `q` quit binding — `q` types a query filter (0/2); Ctrl-C properly aborts. Evidence: .omo/evidence/f3-manual-qa.txt. bash -n clean on fixture + real copies; script SHA-256 1fa78165... unchanged since task-5.
