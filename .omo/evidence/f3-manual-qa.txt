======================================================================
VERDICT: APPROVE
findings: none blocking — all 6 QA items passed with real interactive
evidence in a live tmux nvim session. One documented deviation: the
plan's `:LspInfo` command does not exist on this nvim build (0.11+ has
the builtin `:lsp` command, so nvim-lspconfig's plugin file returns
early and never defines `:LspInfo`); the equivalent `:checkhealth
vim.lsp` was used and shows jdtls attached. All other items verified
exactly as specified.
======================================================================
F3 — Real manual QA for the nvim-config-cleanup-and-java plan
date: 2026-08-16T10:35:00Z  host: DESKTOP-55RSJSA  Linux
HEAD (committed): 5c4bf98  test: add nvim headless smoke test
reviewer: Sisyphus (F3 gate re-run; previous attempt aborted)
config under test: ~/.config/nvim (live, symlinked to repo common/.config/nvim)
sample project: /tmp/opencode/java-qa-sample (T11 fixture, outside repo)
tmux session: f3qa (220x50), nvim booted interactively, all input via send-keys,
all output via `tmux capture-pane -p`

======================================================================
## 0. Setup
$ ls /tmp/opencode/java-qa-sample/src/main/java/com/example/Hello.java
Hello.java   (exists — T11 fixture intact, 16 lines, pom.xml release 21 + junit 5.10.2)
$ ls /usr/lib/jvm/default-java
java-1.21.0-openjdk-amd64   (JDK 21 present)
$ ls ~/.local/share/nvim/mason/packages/ | grep -E '^(jdtls|java-debug-adapter|java-test)$'
java-debug-adapter
java-test
jdtls
==> PASS: prerequisites in place. Booted `nvim Hello.java` in tmux f3qa.
    Stale swap file from the aborted previous F3 run was deleted (option 'd').

======================================================================
## 1. jdtls attaches
Plan says `:LspInfo` — NOT available on this build (see deviation note).
Equivalent used: `:checkhealth vim.lsp` (lspconfig's own alias target).

$ :lua print(vim.inspect(vim.tbl_map(function(c) return c.name end, vim.lsp.get_clients())))
{ "jdtls" }

$ :checkhealth vim.lsp   (excerpt)
vim.lsp: Active Clients
- jdtls (id: 1)
  - Version: 1.60.0-SNAPSHOT
  - Root directory: /tmp/opencode/java-qa-sample
  - Command: { "/home/ms/.local/share/nvim/mason/packages/jdtls/bin/jdtls",
               "-data", "/home/ms/.cache/nvim/jdtls/workspace/java-qa-sample" }
  - Settings: { java = { configuration = { runtimes = { {
      default = true, name = "JavaSE-21", path = "/usr/lib/jvm/default-java" } } } } }
vim.lsp: Active Features — semantic_tokens: Active buffers [1]: jdtls (id: 1)
==> PASS: jdtls attached (id 1), correct root dir, correct runtime.
    Deviation note: `:LspInfo` -> E492: Not an editor command. Root cause:
    nvim-lspconfig plugin/lspconfig.lua line 5-7: `if vim.fn.exists(':lsp') == 2
    then return end` — nvim 0.11+ ships the builtin `:lsp` command, so lspconfig
    skips defining LspInfo/LspLog/etc. `:checkhealth vim.lsp` is the documented
    replacement and shows the same active-client info.

======================================================================
## 2. blink.cmp completion pops
Insert mode inside main(): typed `System.` (line 15, inside class body).

$ tmux capture-pane -p   (excerpt)
     15 ┊   ┊   System.out.println(new Hello().greet("world"));
     16 ┊   }↴       󰜢 class                                                        Class<java.lang.System>   class : Class<java.lang.System>
     17 }↴           󰏿 err                                                                      PrintStream
                     󰏿 in                                                                       InputStream
                     󰏿 out                                                                      PrintStream
                     󰊕 arraycopy(Object arg0, int arg1, Object arg2, int arg3, int…                    void
                     󰊕 clearProperty(String key)                                                     String
                     󰊕 console()                                                                    Console
                     󰊕 currentTimeMillis()                                                             long
                     󰊕 exit(int status)                                                                void
                     󰊕 gc()                                                                            void
==> PASS: blink.cmp menu appeared with real jdtls items (out/err/in as
    PrintStream/InputStream, methods with signatures). Context-aware:
    typing at top level (outside class) offered declaration keywords
    (abstract/class/enum/sealed/...) — jdtls responding correctly per position.

======================================================================
## 3. ;rn / ;ac / gd
$ :map ;rn
n  ;rn         *@<Lua 1340: .../runtime/lua/vim/lsp/buf.lua:716>
                 LSP: [R]e[n]ame
$ :map ;ac
n  ;ac         *@<Lua 591: .../runtime/lua/vim/lsp/buf.lua:1376>
                 LSP: [A]ction [C]ode
$ :map gd
n  gd          *@<Lua 183: .../runtime/lua/vim/lsp/buf.lua:343>
                 LSP: [G]oto [D]efinition

Functional trigger — cursor on `greet` call (14:40), pressed `gd`:
before: NORMAL ... 14:40   (on `new Hello().greet("world")` in main)
after:  NORMAL ... 8:19    (on `public String greet(String name)` — the definition)
==> PASS: all three keymaps bound to vim.lsp.buf.{rename,code_action,definition};
    gd functionally jumped call -> definition via jdtls.

======================================================================
## 4. :Trouble opens
$ :Trouble diagnostics
Window opened (bottom split) showing the jdtls TODO diagnostic tree:
  /tmp/opencode/java-qa-sample/src/main/java/com/example/  1
   └╴ Hello.java  1
     └╴ TODO: localize the greeting Java (536871362) [9, 12]
(Plain `:Trouble` also opened the mode picker: Diagnostics/fzf/loclist/lsp/...)
==> PASS: Trouble diagnostics window opens and lists real jdtls diagnostics.

======================================================================
## 5. :Format works
$ :lua print('Format exists:', vim.fn.exists(':Format'))
Format exists: 2

Functional test — introduced a violation, formatted, verified fix:
$ :lua vim.api.nvim_buf_set_lines(0, 9, 10, false, {'    return    "Hello, " + name + "!";'})
  line 10 became:  return    "Hello, " + name + "!";      (extra spaces)
$ :Format
  line 10 became:  return "Hello, " + name + "!";         (single space — fixed)
No error message; buffer otherwise untouched.
==> PASS: :Format command exists and functionally reformats via conform
    (lsp_format fallback -> jdtls).

======================================================================
## 6. :Mason shows jdtls / java-debug-adapter / java-test
$ :Mason   (UI opened; Installed section, 22 packages)
  ◍ java-debug-adapter
  ◍ java-test
  ◍ jdtls
(also confirmed on disk: ~/.local/share/nvim/mason/packages/{jdtls,java-debug-adapter,java-test})
==> PASS: all three java packages installed and shown in the Mason UI.

======================================================================
## Cleanup
$ :qa!  ;  tmux kill-session -t f3qa
Session gone, no leftover nvim processes. No config/repo files modified
(only the /tmp/opencode sample buffer was edited in-session and discarded
with :qa! / :e!; Hello.java on disk unchanged at 16 lines).
======================================================================