**Keep filesystem searches narrow.** Never run a whole-disk scan (e.g. `find / …`). Search only in specific, narrow directories (the project tree, `~/.claude`, `~/.config`, `~/.agents`, `~/.pi`). Whole-disk searches are slow and disruptive.

